name: CMake

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Debug

jobs:
  #  linux:
  #    name: Run on Ubuntu latest
  #    runs-on: ubuntu-latest
  #
  #    steps:
  #      - uses: actions/checkout@v2
  #
  #      - name: Run linux.sh
  #        run: linux.sh
  #        shell: bash
  #
  #      - name: Create Build Directory
  #        run: mkdir build
  #
  #      - name: Configure CMake
  #        run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DCMAKE_TOOLCHAIN_FILE=D:/dev/vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x86-windows
  #
  #      - name: Build
  #        run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}} -DCMAKE_TOOLCHAIN_FILE=D:/dev/vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x86-windows

  windows:
    name: Run on Windows latest
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: install dependencies
        run: |
          choco install cmake
          cmake --version

      - name: Run windows.bat
        run: ./windows.bat
        shell: pwsh

      - name: Create Build Directory
        run: mkdir cmake-build-mingw-x64

      - name: Build
        run: cmake --preset "MinGW x64-debug"
#        run: cmake -G "CodeBlocks - MinGW Makefiles" -DCMAKE_TOOLCHAIN_FILE=${{github.workspace}}vcpkg\scripts\buildsystems\vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x64-windows

      - name: Run
        #        run: cmake --target Hazelchain --preset "MinGW x64"
        run: cmake --build --preset "MinGW x64"
